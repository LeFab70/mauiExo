<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:BookCatalog.ViewModels"
             xmlns:local="clr-namespace:BookCatalog"
             x:Class="BookCatalog.Views.BooksListPage"
             x:DataType="vm:BookListViewModel"
             Title="Livres - Fabrice"
             BindingContext="{x:Static local:App.MainViewModel}">
    <CollectionView ItemsSource="{Binding Books}"
                    SelectedItem="{Binding SelectedBook, Mode=OneWayToSource}"
                    SelectionMode="Single"
                    Margin="10"
                    SelectionChanged="CollectionView_SelectionChanged">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="vm:BookViewModel">
                <SwipeView>
                    <SwipeView.RightItems>
                        <SwipeItems>
                            <SwipeItem Text="Supprimer"
                                       BackgroundColor="Red"
                                       Command="{Binding DeleteBookCommand, Source={x:Static local:App.MainViewModel}}"
                                       CommandParameter="{Binding}" />
                        </SwipeItems>
                    </SwipeView.RightItems>

                    <VerticalStackLayout Padding="10">
                        <Label Text="{Binding Title}"
                               FontSize="16" />
                    </VerticalStackLayout>
                </SwipeView>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>